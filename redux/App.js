import React from 'react';
import {SafeAreaView} from 'react-native';
import Counter from './components';

const App = () => {
  return (
    <SafeAreaView>
      <Counter />
    </SafeAreaView>
  );
};

export default App;
